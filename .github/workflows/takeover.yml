- name: Run scan
        run: |
          export PATH=$PATH:$(go env GOPATH)/bin
          subfinder -dL domains.txt -silent -o subs.txt
          nuclei -l subs.txt -t takeovers -o results.txt || true

      - name: Show results
        run: cat results.txt || echo "No results found"
